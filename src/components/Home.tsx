import Form from "./Form";
import List from "./List";
import Layout from "./Layout";

const Home = () => {
  return (
    <Layout>
      <Form />
      <List />
    </Layout>
  );
};

export default Home;
